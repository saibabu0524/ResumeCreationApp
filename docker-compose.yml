version: '3.8'

services:
  backend:
    build:
      context: ./backend
    container_name: resume_backend
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    networks:
      - resume-net

networks:
  resume-net:
    driver: bridge

